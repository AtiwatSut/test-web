{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "push": "git add . && git commit -m 'deploy' && git push origin mian",
    "deploy": "npm run generate && gh-pages -d .output/public"
  },
  "dependencies": {
    "@mdi/font": "^7.4.47",
    "nuxt": "^3.16.2",
    "vite": "^6.3.4",
    "vue": "^3.5.13",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite-plugin-vuetify": "^2.1.1",
    "vuetify": "^3.8.4"
  }
}
